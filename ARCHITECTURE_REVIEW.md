# アーキテクチャ・レビュー

## 総評

- レイヤード／クリーンアーキテクチャの層の名前だけを採用した結果、業務ロジックの切り出しと関心の分離に失敗しているのが所感。

## 症状

### 依存方向

- 依存性の逆転違反が多くみられる
  - 依存方向が内向きに保てていない。最も顕著なのはドメイン層が外部SDKの具象に依存している
- xxClientの定義がドメイン層に定義されている
  - 外部のClientなど、インスタンスはinfraか、platformなどに切り出しておきたい。
- ドメイン層のrepositoriesパッケージの定義が技術詳細的な観点が漏れ出していて、抽象化できていない。

### 責務配置

- infra層のHandleXXX関数の責務の集約・関心の分離が不十分
  - パース
  - ファイルサイズ・件数の判定
    → ファイルサイズはhandler, 件数はusecaseに任せるべき。
  - 責務が多すぎる（画像の分別、読み取り、レスポンス内容の直作成）
- ドメイン層で定義している生成条件などのエンティティはいいが、それを作成するのがinfra層という横断的なレイヤー侵害
- ドメイン層にvalueobjectsというパッケージの存在。
  - 型の種類 = 業務概念 ではない。
  - 画像エンティティとして切り分けたい。

### 実装の正しさ

- usecase層で複数枚生成のジョブをgo routineで待つのはいいけど、生成結果を保存するチャネルにバッファがない
  - いずれかのループでerrが発生すると結果受信チャネルは永遠にブロックされてリークする。
  - 並列でI/O待ちの多いジョブをgoroutineで実装するのはいいが、errGroupとバッファ付きチャネルなどの組み合わせでリファクタリングが必要。
- isQuotaErrorなどのエラー内容の判定がまさかのstrings.Contains。
  - 一文字ずれたりするだけで破綻する脆い契約。
  - それを fmt.Errorf("service temporarily unavailable due to high demand: %w", err) でラップしてそのままHandlerに返す。
  - エラーの内容や性質ではなく、結果の有無などの状況判断で分岐しておりユーザー・デベロッパーの誰にも優しくない

### 契約欠如

- Handlerがレスポンスをハンドメイド（map[string]any）で返す
  - DTOとしてのレスポンスの契約がないので、FEとの関係性において静的言語としてのメリットを失っているに等しい。
  - 応答契約としての構造体を定義し、Usecase → Handlerで変換をし、返却するべき。

## 直す順序（依存の根→葉）

症状の列挙順ではなく、依存の根（内向き依存の是正）から葉へ。1-2で矢印を正し、3-4で境界を痩せさせ、5-6で契約を形式化、7は独立修正、8は構造命名。

| # | やること | 解消する観点・項目 |
|---|---|---|
| 1 | 境界interfaceを「使う側」へ移設（domainから追い出す） | 依存方向（依存性の逆転違反 / xxClient / repositories抽象化） |
| 2 | `domain/services` を再分類（実体はUseCase） | 依存方向・責務配置の前提 |
| 3 | エンティティ/値オブジェクト生成をUseCaseに集約 | 責務配置（infra層がエンティティ作成） |
| 4 | Handlerを `decode→usecase→encode` に縮退＋弾く責務の再配置 | 責務配置（HandleXXX責務過多） |
| 5 | エラー契約を型付け（sentinel + `errors.Is`） | 実装の正しさ（strings.Contains脆い契約） |
| 6 | 応答DTO化（`map[string]any` 廃止） | 契約欠如（ハンドメイドレスポンス） |
| 7 | goroutineを安全化（errgroup＋bounded） | 実装の正しさ（チャネルバッファ無しリーク） |
| 8 | パッケージ命名/構造（inbound/outbound分離） | 責務配置（valueobjects命名）＋構成 |
